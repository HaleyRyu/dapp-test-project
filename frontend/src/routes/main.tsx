import React, {FC} from "react";
import Box from "@mui/material/Box";
import Button from "@mui/material/Button";

const Main: FC = () => {
    return <Box>Main <Button>Button</Button></Box>;
};

export default Main;